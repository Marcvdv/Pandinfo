spring.datasource.url=jdbc:mysql://localhost:3306/pandinfo?useSSL=false
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.username=pandinfo
spring.datasource.password=
spring.jpa.database_platform=org.hibernate.dialect.MySQL5Dialect

#NOT IN PRODUCTION. NICE IN DEVELOPMENT!!!
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create-drop
#OPTIES
# validate   : validate the schema, makes no changes to the database.
# update     : update the schema.
# create     : creates the schema, destroying previous data.
# create-drop: drop the schema at the end of the session.